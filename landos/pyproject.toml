[project]
name = "landos"
version = "0.1.0"
description = "BaseMod LandOS — Event Mesh"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0,<3.0",
    "shapely>=2.0,<3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
