---
source: llm-wiki
generator: landos-wiki
generated_at: {{ generated_at }}
staleness_days: 7
subdivision_id: {{ sub.subdivision_id }}
vacancy_ratio: {{ sub.vacancy_ratio }}
sources:
  - "subdivisions.subdivision_id:{{ sub.subdivision_id }}"
tags:
  - wiki
  - subdivision
  {% if sub.stall_flag %}- stalled{% endif %}
---

# Subdivision — {{ sub.name }}

> Auto-generated by LandOS Wiki Ingestor. Do not edit above the Human Notes line.

**Generated:** {{ generated_at }}

## Summary

| Field | Value |
|-------|-------|
| **Name** | {{ sub.name }} |
| **Municipality** | {{ sub.municipality_id or "Unknown" }} |
| **County** | {{ sub.county or "Unknown" }} |
| **Total Lots** | {{ sub.total_lots or 0 }} |
| **Vacant Lots** | {{ sub.vacant_lots or 0 }} |
| **Vacancy Ratio** | {{ "%.0f"|format((sub.vacancy_ratio or 0) * 100) }}% |
| **Stalled** | {{ "Yes" if sub.stall_flag else "No" }} |
| **Stall Score** | {{ "%.2f"|format(sub.stall_score or 0) }} |
| **Infrastructure** | {{ sub.infrastructure_status or "Unknown" }} |
| **Plat Date** | {{ sub.plat_date or "Unknown" }} |

{% if linked_opportunities -%}
## Linked Opportunities

| Name | Tier | Score | Lots | Active |
|------|------|-------|------|--------|
{% for opp in linked_opportunities -%}
| [[Opportunity — {{ opp.name }}]] | {{ opp.precedence_tier }} | {{ "%.3f"|format(opp.composite_score) }} | {{ opp.lot_count }} | {{ "Yes" if opp.has_active_listings else "No" }} |
{% endfor %}
{% endif %}

{% if listing_summary -%}
## Listing Activity

| Status | Count | Avg Price |
|--------|-------|-----------|
{% for row in listing_summary -%}
| {{ row.status }} | {{ row.count }} | {{ "${:,.0f}".format(row.avg_price) if row.avg_price else "N/A" }} |
{% endfor %}
{% endif %}

---

## Human Notes
<!-- Add manual observations below this line. They survive re-generation. -->
